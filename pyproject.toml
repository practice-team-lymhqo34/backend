[project]
name = "logiflow"
version = "0.1.0"
description = "LogiFlow project for logistics management API"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.3",
    "uvicorn[standard]>=0.34.0"
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "ruff>=0.15.9",
]
